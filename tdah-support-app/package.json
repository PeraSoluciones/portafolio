{
  "name": "tdah-support-app-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:api": "npm run dev --workspace=api",
    "dev:mobile": "npm run start --workspace=mobile",
    "bootstrap": "npm install"
  },
  "devDependencies": {}
}